{% extends "admin/index.html.twig" %}
{% block datatable %}
	<h2>Sources</h2>
	<div class="table-responsive">
		<table class="table table-striped table-sm">
			<thead>
				<tr>
					<td>Title</td>
					<td>Url</td>
					<td></td>
				</tr>
			</thead>
			<tbody>
			{% for result in results %}
				<tr>
					<td><a href="/admin/source/{{result.id}}">{{result.title}}</a></td>
					<td>{{result.url}}</td>
					<td><a href="/admin/source/{{result.id}}"><input type="button" value="Edit"></a></td>
				</tr>
			{% endfor %}			
			</tbody>
		</table>
	</div>
	
{% endblock %}
