{% extends "base.html.twig" %}
{% block content %}
	<nav class="navbar navbar-dark fixed-top bg-dark flex-md-nowrap p-0 shadow">
      <a class="navbar-brand col-sm-3 col-md-2 mr-0" href="{{ path('Admin') }}">Product Search Admin</a>
      <ul class="navbar-nav px-3">
        <li class="nav-item text-nowrap">
          <a class="nav-link" href="{{ path('Search_Form')}}">Return to Search</a>
        </li>
      </ul>
    </nav>
    <div class="container-fluid">
      <div class="row">
        <nav class="col-md-2 d-none d-md-block bg-light sidebar">
          <div class="sidebar-sticky">
            <ul class="nav flex-column">
				<h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
				  <span>Sources</span>
				  <a class="d-flex align-items-center text-muted" href="{{ path('Sources')}}">
					<span data-feather="plus-circle"></span>
				  </a>
				</h6>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('Sources')}}">
					<span data-feather="file-text"></span>
					View Named Sources
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('Sources', {'all':'true'} )}}">
					<span data-feather="file-text"></span>
					View All Sources
					</a>
				</li>				<li class="nav-item">
					<a class="nav-link" href="{{ path('Source_Form')}}/0">
					<span data-feather="file-text"></span>
					Add Source
					</a>
				</li>
				<h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
				  <span>Products</span>
				  <a class="d-flex align-items-center text-muted" href="{{ path('Products')}}">
					<span data-feather="plus-circle"></span>
				  </a>
				</h6>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('Products')}}">
					<span data-feather="file-text"></span>
					View Product
					</a>
				</li>
				<h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
				  <span>Crawl</span>
				  <a class="d-flex align-items-center text-muted" href="#">
					<span data-feather="plus-circle"></span>
				  </a>
				</h6>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('Robots_UpdateAll')}}">
					<span data-feather="file-text"></span>
					Scan Next Batch of Sources for Products
					</a>
				</li>
				{% if((admin is defined) and (admin)) %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('Robots_ResetAll')}}">
					<span data-feather="file-text"></span>
					Clear database
					</a>
				</li>
				{% endif %}		
			</ul>
		</div>
	</nav>        
	<main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-4">
          
		{% block datatable %}{% endblock %}	
          
	</main>
	</div>
	</div>
{% endblock %}
